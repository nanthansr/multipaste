# Context vault

This folder is the repo-local second brain for Multipaste.

Read `context/manifest.md` first when you need a quick overview of the codebase.

Regenerate the manifest with:

`swift run contextbrain`

The generator scans the source tree, computes file metadata, and rewrites the manifest so the summary stays in sync with code changes.